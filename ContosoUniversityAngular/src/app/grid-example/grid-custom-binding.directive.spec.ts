import { GridCustomBindingDirective } from './grid-custom-binding.directive';

describe('GridCustomBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new GridCustomBindingDirective();
    expect(directive).toBeTruthy();
  });
});
